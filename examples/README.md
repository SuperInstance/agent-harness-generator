# Examples

Real, runnable patterns showing how to use `agent-harness-generator`.

| Example | What it shows |
|---|---|
| [`multi-host/`](./multi-host/) | One harness targeting Claude Code + Codex with the same kernel |
| [`federation/`](./federation/) | Two harness instances coordinating via the kernel's federation transport |

More examples queued for the next iterations.
